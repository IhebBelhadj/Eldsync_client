import { ExtendedTooltipDirective } from './extended-tooltip.directive';

describe('ExtendedTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ExtendedTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
